# Fixture run — 2026-09-17 — partial: one fixture

**Not a full fixture run.** One fixture changed today, and this page records the run made against
it. The other 18 fixtures are byte-for-byte unchanged (`fixtures/_build.py` rebuilt the generated
ones, and `git diff` showed no change outside `broken-04-sector`), and `rules.md` did not change, so their last results in `fixtures-2026-09-11.md` and
the blind runs still apply.

| Fixture | What changed | Run | Expected | Result |
| --- | --- | --- | --- | --- |
| `broken-04-sector` | Executive Summary now describes freight logistics throughout, so the fixture plants one fault, not a violation plus a contradiction | blind case 12, staged outside the repository | `R-05` `FAIL · NON-CONFORMANT` | `FAIL · NON-CONFORMANT` ✅ · no contradiction reported ✅ |

One row differed from the key: `R-08` came back `CONFIRMED` where the key expects `TO CHECK`.
The key was citing a rule `rules.md` does not contain, so this is recorded as an open gap, not as
a failure of the run. See `blind-case-12-2026-09-17.md` and `../README.md`, *What broke*.
