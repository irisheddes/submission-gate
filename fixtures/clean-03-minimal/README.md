# Fixture: clean-03-minimal — conformant

**Cartulary is a fictional company.** Every fact here was invented for this repository.

A **passing** package. The auditor must return no `FAIL` and no `PARTIAL`.

| | |
| --- | --- |
| **Requirement exercised** | `—` |
| **Expected** | Identical to the control: `R-01`–`R-09` `PASS`, `R-10` `NOT APPLICABLE` |
| **How it differs from the control** | A far shorter package that still satisfies every requirement: a 2-page Executive Summary, a 31-second video, 19 employees, and on-site presence exactly at the 3-day minimum. Tests that brevity and bare compliance are not read as gaps — an auditor that equates 'short' with 'incomplete' fails here. |

Four of the twelve fixtures in this folder conform. They are here because an auditor is not
proved by what it catches — a gate that fires on everything catches every violation and is
useless. These test that it stays quiet on packages that are unusual and still correct.
